# Example environment variables for Docker Compose
DB_HOST=db
DB_USER=carouser
DB_PASS=caropass
DB_NAME=carochat

# MySQL initial root password (only used by the mysql container during init)
MYSQL_ROOT_PASSWORD=rootpassword
